process TEST = {
	path p = { get }
	es_source = PoolDBESSource {
                            VPSet toGet = {
                            {string record = "mySiStripNoisesRcd"
                             string tag = "noise_tag"
                             } }
                             string connect = "sqlite_file:pippo3.db"
                             string timetype = "runnumber" 
                             untracked string BlobStreamerName = "DefaultBlobStreamingService"
                             PSet DBParameters = {
                               untracked int32 messageLevel=0
                             }
			    }
        source = EmptyIOVSource {
          string timetype = "runnumber"
          untracked uint32 firstRun = 1
	  untracked uint32 lastRun = 10
          uint32 interval = 1
        }
        service = MessageLogger { 
          untracked vstring destinations = { "debug.txt" }
          untracked vstring debugModules = { "*" }
          untracked PSet debug.txt = {
                        untracked string threshold = "DEBUG"
	  }
        }
	module get = EventSetupRecordDataGetter { VPSet toGet = {
	       {string record = "mySiStripNoisesRcd"
	        vstring data = {"mySiStripNoises"} } 
	       }
	       untracked bool verbose = true 
	}

       # module print = AsciiOutputModule { }
       # endpath ep = { print }
}
