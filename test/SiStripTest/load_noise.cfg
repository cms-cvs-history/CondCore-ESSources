# Configuration file for EventSetupTest_t

process TEST = {

	path p = { get }


	es_source = PoolDBESSource { VPSet toGet = {
                                   {string record = "mySiStripNoisesRcd"
                                     string tag = "blobtest"
                                    } }
		    		    bool loadAll = true
                                    string connect = "sqlite_file:mydb_Noise.db"
                                    untracked string catalog = "file:mycat.xml"
			            string timetype = "runnumber" 
                                    untracked uint32 authenticationMethod = 1
                                    untracked bool loadBlobStreamer = true 
				   }
	

	source = EmptySource {untracked int32 maxEvents = 20 untracked uint32 firstRun = 1 untracked uint32 numberEventsInRun = 10}

	module get = EventSetupRecordDataGetter { VPSet toGet = {
	       {string record = "mySiStripNoisesRcd"
	        vstring data = {"mySiStripNoises"} } 
	       }
	       untracked bool verbose = true 
	}

        module print = AsciiOutputModule { }
        endpath ep = { print }
}
