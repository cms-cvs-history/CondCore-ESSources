process E2E = {
# empty input service, fire 10 events
	source = EmptyInputService {untracked int32 maxEvents = 10}
	es_source = TrackerPedestalsRetriever{string 
connect="sqlite_file:trackerped.db" string iovname="TrackerCalibration"}
#create a small analyzer
        module prod = TrackerCalibAnalyzer {}
#provide a scheduler path
        path p1 = { prod }	
}
